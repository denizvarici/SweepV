<Window x:Class="SweepV.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SweepV" Height="500" Width="700">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Path input row -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Path:" VerticalAlignment="Center" Width="50"/>
            <TextBox Text="{Binding PathToScan, UpdateSourceTrigger=PropertyChanged}"
                      Width="450" VerticalAlignment="Center"/>
            <Button Content="Scan" Command="{Binding ScanCommand}"
                    Width="80" Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Status row -->
        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}"
                   Margin="0,0,0,8" FontWeight="Bold"/>

        <!-- Results header -->
        <TextBlock Grid.Row="2" Text="Results:" Margin="0,0,0,4"/>

        <!-- Results list -->
        <ListView Grid.Row="3" ItemsSource="{Binding ScanResult.Children}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="300"
                                    DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Is Folder" Width="80"
                                    DisplayMemberBinding="{Binding IsDirectory}"/>
                    <GridViewColumn Header="Size (MB)" Width="120"
                                    DisplayMemberBinding="{Binding SizeInMb, StringFormat=F2}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>